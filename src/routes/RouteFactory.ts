import { Express } from 'express';
import { SQLiteHelper } from '../database/SQLiteHelper.js';

export class RouteFactory {
    constructor(app: Express, db: SQLiteHelper) {

    }

    public factory(): void {
        
    }
}